install:
  - os: [linux, darwin]
    shell: [zsh, bash, sh]
    dependency: [echo]
    step:
      - name: Step 1
        run: echo install for unix
      - sleep 2
      - echo install for unix

uninstall:
  - os: [linux, darwin]
    shell: [zsh, bash, sh]
    dependency: [echo]
    step:
      - echo uninstall for unix
